/** Default wordmark until an admin sets workspace branding. */
export const APP_NAME = "Project Tracker";

/** Placeholder workspace until sign-in and workspaces arrive in M1. */
export const PREVIEW_WORKSPACE_SLUG = "demo";
