# Check out and commit text files with LF so Prettier's default `endOfLine: "lf"`
# passes on Windows clones too (core.autocrlf=true would otherwise write CRLF).
* text=auto eol=lf

*.ico binary
*.png binary
*.jpg binary
*.woff2 binary
